%h3 Offers / Requests
%table.table.table-condensed
  %thead
    %tr
      %th Title
      %th Category
      %th Created
  %tbody
    - @ads.each do |ad|
      %tr{:class => "#{ad.offer ? 'success' : 'warning'}"}
        %td= ad.category_id
        %td=link_to ad.title, ad_path(ad)
        %td Created by #{link_to ad.user.username, user_path(ad.user)} #{time_ago_in_words ad.created_at}
= link_to 'New Ad', new_ad_path
